// No exemplo abaixo, queremos somar valores de 1 até o número passado para a função.

const n = 5;

let resultado = 0;
for (let index = 0; index <= n; index += 1) {
  // `"?"`
  resultado += index;
};
console.log(resultado)
